name: web_reader
description: "WebReader – Reader Mode + TTS app for Android."
publish_to: 'none'

version: 1.0.4+4

environment:
  sdk: '>=3.7.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_riverpod: ^2.5.1
  http: ^1.2.0
  html: ^0.15.4
  sqflite: ^2.3.2
  path: ^1.9.0
  flutter_tts: ^4.2.5
  receive_sharing_intent: ^1.8.1
  share_plus: ^10.1.4
  uuid: ^4.5.3
  shared_preferences: ^2.3.0
  audio_service: ^0.18.18
  rxdart: ^0.28.0
  package_info_plus: ^8.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  flutter_launcher_icons: ^0.14.3

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/appicon2.png

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package

flutter_launcher_icons:
  android: true
  ios: false
  image_path: "assets/appicon2.png"
  # Adaptive icons (Android 8+ / API 26+): prevents blurry scaled legacy PNG.
  # The foreground is your icon; background is a solid color behind it.
  # The foreground will be scaled to fill 108×108dp with a 72dp safe zone —
  # if your icon has important content at the edges, add padding to appicon.png.
  adaptive_icon_background: "#FFFFFF"
  adaptive_icon_foreground: "assets/appicon2.png"
